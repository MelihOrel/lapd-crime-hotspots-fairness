# ============================================================================
# requirements.txt
# Versions below are the exact ones the pipeline was validated against
# (end-to-end on the real 974,477-row dataset). Lower bounds are given because
# the geospatial stack (libpysal / esda / geopandas) is version-sensitive and
# these majors are known to interoperate.
# ============================================================================

# --- core data ---
pandas>=2.2,<4.0
numpy>=1.26
pyarrow>=15.0          # parquet engine

# --- machine learning ---
scikit-learn>=1.4
lightgbm>=4.3

# --- responsible ML ---
shap>=0.45
fairlearn>=0.10

# --- geospatial / hotspot statistics (version-sensitive — keep aligned) ---
geopandas>=0.14
libpysal>=4.9
esda>=2.5

# --- mapping & visualisation ---
folium>=0.16
matplotlib>=3.8
seaborn>=0.13

# --- config ---
pyyaml>=6.0
